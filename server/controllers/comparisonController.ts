import express from 'express';

export const getEventsToCompare = async (req: express.Request, res: express.Response) => {
  console.log('test');
  res.send('test');
}

export const postEventToCompare = async (req: express.Request, res: express.Response) => { }
